def distance_from_zero(arg):
    return type(arg) == int or type(arg) == float
    if type == int or float:
        return abs(-10)
    else:
        return "Nope"
"""It says Oops, try again. Your function seems to fail on input -10 when it returned 'True' instead of '10' and i dont even have a true anywhere in the code"""